
namespace DesafioEntregable1
{
    public class Program
    {
        /*static void Main(string[] args)
        {
            Usuario usuario = new Usuario();
            string nombre = usuario.Nombre;
            Console.WriteLine(nombre);

            Dictionary<int, Usuario> diccionario = new Dictionary<int, Usuario>();

            diccionario.Add(1, new Usuario());
            diccionario.Add(2, new Usuario());
            diccionario.Add(3, new Usuario());

            foreach (var user in diccionario)
            {
                Console.WriteLine("Ingrese un nombre");
                user.Value.Nombre = Console.ReadLine();
            }

            foreach (var user in diccionario) 
            { 
                Console.WriteLine(user.Value.Nombre); 
            }
        }
        */
    }
}
