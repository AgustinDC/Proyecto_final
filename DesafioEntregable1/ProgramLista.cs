
namespace DesafioEntregable1
{
    public class ProgramLista
    {        
        static void Main(String[] args)
        {
            //Programa para recorrer una lista.
            /*List<Usuario> listaDeUsuarios = new List<Usuario>();

            while (usuario.Nombre.ToUpper() != "FIN")
            {
                Console.WriteLine("Ingrese el nombre del nuevo usuario o fin para finalizar la carga");
                Usuario usuario = new Usuario();
                usuario.Nombre = Console.ReadLine();
                if (usuario.Nombre.ToUpper() != "FIN")
                {
                    listaDeUsuarios.Add(usuario);
                }
            }

            foreach (var usuario1 in listaDeUsuarios)
            {
                if (usuario1.Nombre.ToUpper() == "AGUSTIN")
                {
                    Console.WriteLine("Encontre a AGUSTIN, se termino");
                    break;
                }
            }*/
            
        }
    }
}
